<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>kwikBlog</title>
    <!-- Include Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" rel="stylesheet">
    <!-- Include own CSS for customization -->
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <div style="background-color:#DCEDC8">
        <header class="pv-header">
            <nav class="nav nav-pills pv-nav">
                <a class="nav-link pv-nav" href="/">Home</a>
                <a class="nav-link pv-nav" href="/dashboard">Dashboard</a>
                {{#unless loggedIn}}
                <a class="nav-link pv-nav" href="/login">Login</a>
                {{/unless}}
                <a id="logout" class="nav-link pv-nav" href="/logout">Logout</a>
            </nav>
        </header>
        <main>
            <!-- Render the sub layout -->
            {{{ body }}}
        </main>
        <footer class="pv-footer">
            <p class="bg-gray-300 p-10 bg-4 text-center text-3xl "> kwikBlog is made with &#x2764;&#xFE0F;
                by PV
            </p>
        </footer>
    </div>
    <!-- Render script for logged in users only -->
    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>